# Backend Environment Variables for Production
NODE_ENV=production
PORT=3000

# Database Configuration
DATABASE_URL=file:./dev.db

# JWT Configuration - CHANGE THESE IN PRODUCTION!
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-change-this-in-production
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
FRONTEND_URL=https://your-coolify-domain.com

# Optional: Database URL for PostgreSQL (if switching from SQLite)
# DATABASE_URL=postgresql://username:password@localhost:5432/dialcraft

# Optional: Redis URL for session management (future enhancement)
# REDIS_URL=redis://localhost:6379